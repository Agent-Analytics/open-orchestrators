{
  "name": "awesome-multi-agent-orchestrators",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "clean": "node -e \"import('node:fs').then(({rmSync}) => ['dist', '.astro'].forEach((path) => rmSync(path, { recursive: true, force: true })))\"",
    "build": "astro build",
    "rebuild": "npm run clean && npm run build",
    "fetch:github-stars": "node scripts/fetch-github-stars.mjs",
    "test": "node --test",
    "preview": "astro preview"
  },
  "dependencies": {
    "@agent-analytics/shared-ui": "0.3.5",
    "astro": "^6.0.1"
  }
}
